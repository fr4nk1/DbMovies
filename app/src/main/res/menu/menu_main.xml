<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".ui.main.MainActivity">

    <item
        android:id="@+id/menu_sort_alpha"
        android:title="Sort Alpha"
        android:icon="@drawable/ic_sort_by_alpha"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/menu_sort_vote"
        android:title="Sort Vote"
        android:icon="@drawable/ic_sort_vote"
        android:visible="false"
        android:enabled="false"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/menu_flip"
        android:title="Flip"
        android:icon="@drawable/ic_story"
        app:showAsAction="ifRoom"/>

    <item
        android:id="@+id/menu_info"
        android:title="Info"
        android:icon="@drawable/ic_info"
        app:showAsAction="ifRoom"/>

</menu>